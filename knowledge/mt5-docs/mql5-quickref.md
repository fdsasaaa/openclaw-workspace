# MQL5快速参考

## 核心函数
- OrderSend() - 发送订单
- OrderModify() - 修改订单  
- OrderClose() - 平仓
- PositionSelect() - 选择持仓

## 数据类型
- int, double, string, bool, datetime

## 事件函数
- OnInit() - 初始化
- OnTick() - 每tick执行
- OnDeinit() - 卸载
